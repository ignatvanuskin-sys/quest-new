# Аудит: CRO, UX, мобильные, доступность, производительность и QA

Документ фиксирует, что именно проверено, что найдено и что исправлено.
Все замеры — на production-сборке (`npm run build && npm run start`), локально,
в реальном Chrome.

---

## 1. CRO-аудит (по вопросам брифа)

| Вопрос | Ответ | Как решено |
|---|---|---|
| Понятно ли за 3 секунды, что продаёт сайт? | **Да** | hero: «НЕ ЗАХОДИ ОДИН.» + подпись «хоррор-перформанс в реальном бомбоубежище» + 4 факта (оценка, площадь, длительность, размер команды) + две кнопки |
| Понятно ли, насколько страшный квест? | **Да** | фильтр по страху в каталоге, шкала 1–10 у каждого квеста и отдельный блок с 4 реальными уровнями (без актёров → полный контакт) |
| Видна ли цена? | **Да** | цена «от 3 500 ₸» на каждой карточке, полный пересчёт в корзине на шаге 3, тарифы прямо в форме |
| Понятно ли количество игроков? | **Да** | «2–15 чел.» на карточке, фильтр «сколько вас», счётчик игроков с автоматическим пересчётом цены |
| Понятно ли время? | **Да** | 60 минут в трёх местах; слоты с точным временем старта; прошедшие слоты не показываются кликабельными |
| Видна ли бронь? | **Да** | CTA в хедере, в hero, на каждой карточке, липкая кнопка на мобильных, финальный экран, футер |
| Есть ли social proof? | **Да** | 13 реальных отзывов, оценка 9.9/10, ссылки на 2ГИС и Instagram для проверки, фото эмоций после игры |
| Сняты ли главные возражения? | **Да** | блок «Перед входом» (8 карточек) + FAQ из 14 вопросов + явное «стоп-слово работает всегда» |
| Есть ли причина забронировать сейчас? | **Да, честная** | показываются реальные свободные слоты («сегодня 19:30, 2 места») на основе подтверждённых броней |
| Не слишком ли сложная форма? | **Нет** | 3 экрана вместо 6 шагов, дата и время на одном экране, на мобильных проходится одной рукой |

### Что сознательно НЕ сделано (анти-манипуляции)

* нет таймеров «осталось 2 минуты» и фейкового дефицита;
* нет «10 000 довольных гостей» и прочих непроверяемых цифр;
* цены не спрятаны за «уточните у менеджера» — тарифы указаны прямо;
* отмены и опоздания описаны честно, а не спрятаны в конце;
* один отзыв в карусели — с замечанием («нас даже не пытались утащить»), он честнее
  двадцати восторженных и повышает доверие.

### Что мешало конверсии и было исправлено

| Проблема | Исправление |
|---|---|
| Красный цвет текста (`#b0121b`) на почти чёрном фоне давал 2.86:1 — заголовок и подписи «плыли» | токен `--color-crimson` заменён на `#dd5560` (5.37:1) — теперь CTA-акценты читаются, а тёмный «кровавый» остался в градиентах кнопок |
| Форма требовала JS без запасного пути | добавлен `<noscript>` с телефоном и WhatsApp |
| При ошибке сервера/сети человек оставался в тупике | описано, что данные не потеряны, и предложен WhatsApp |
| Дубли брони от одного клиента | сервер находит существующую бронь и показывает её вместо создания второй |

---

## 2. UX-аудит: что проверено в браузере

Ширины: **320, 375, 390, 430, 768, 1280, 1440, 1920**.

| Проверка | Результат |
|---|---|
| Горизонтальный скролл | нет ни на одной ширине (320 → 310, 375 → 365, 1440 → 1430, 1920 → 1910) |
| Битые изображения | нет |
| Наложение липкой панели брони на контент | нет (проверено на максимальном скролле: перекрытых элементов 0) |
| Обрезка текста липкой панелью | было — сокращён текст до «от 3 500 ₸ · есть слоты» |
| Липкое меню при скролле | корректно: прозрачное сверху, тёмное стекло после 48 px |
| Кастомный курсор | подписи `[ ОТКРЫТЬ ]`, `[ ЗАБРОНИРовать ]` и hover-слой «ENTER EXPERIENCE» работают |
| Залипшая подпись курсора после интро | было (`[ SKIP ]` оставался) — исправлено проверкой элемента под курсором раз в ~20 кадров |
| Интро | не дольше 4.3 с, кнопка «Пропустить» доступна сразу, при `prefers-reduced-motion` не показывается |
| Звук | по умолчанию выключен, включается только кликом, синтезируется на месте (без аудиофайлов) |
| Бронь до конца | пройдена на 375: заявка `HC-X3LXNAQB` создалась, экран успеха показал номер, адрес, сумму |
| Ошибки формы | пустая отправка → `aria-invalid` на имени, телефоне и согласии, красный текст ошибок, «дрожание» кнопки |
| Админка | вход по паролю работает, видны 3 брони, счётчики (новых 2, сегодня 1, сумма 50 000 ₸) и смена статуса |

---

## 3. Доступность

### Контраст (замерено на фоне `rgb(5,5,7)`)

| Элемент | Цвет | Размер | Контраст | Порог | Итог |
|---|---|---|---|---|---|
| Hero «НЕ ЗАХОДИ» | `rgb(230,225,216)` | 48.75 px | 15.64:1 | ≥3 | ✅ |
| Hero «ОДИН.» | `rgb(221,85,96)` | 56.25 px | 5.37:1 | ≥3 | ✅ |
| Эйрбрау «КАТАЛОГ», «УРОВЕНЬ СТРАХА», «ОТЗЫВЫ» | `rgb(224,98,107)` | 10 px | 5.94:1 базовый, **5.02–5.15 с учётом всех слоёв** | ≥4.5 | ✅ |
| Вторичный серый (`--color-ash-text`) | `rgb(148,143,134)` | 9–10 px подписи | 6.11 базовый, **5.37 с учётом слоёв** | ≥4.5 | ✅ |
| Подписи SVG-схемы входа | `rgb(179,174,166)` | 10–11 px | **7.49–14.17** | ≥4.5 | ✅ |
| Прочее (`--color-dust`) | — | только декоративные разделители и отключённые даты | — | — | допустимо |

До исправлений красный текст давал **2.86:1**, серый «/10» — **2.2:1**, а после добавления
эффекта затемнения нижняя часть страницы падала до **1.34:1**. Все три проблемы исправлены
сменой токенов и геометрии затемнения, а не точечными заплатками; итоговый минимум — **5.02:1**.

### Тап-таргеты (375 px)

До: чипы фильтров 35 px, гамбургер 38 px, стрелки отзывов 38 px, CTA карточки 40 px,
ссылка в подвале 15 px, логотип 36 px.
После: **все проверенные элементы ≥ 44 px** (чипы 44, гамбургер 44×44, стрелки 44×44,
CTA карточки 44, подвал 44, логотип 36×44, hero-CTA 56, нижняя панель 68).

### Остальное

* видимый фокус на всех интерактивных элементах (`:focus-visible`, 2 px + отступ);
* skip-link «Перейти к квестам» для клавиатуры;
* мобильное меню: `role="dialog"`, `aria-modal`, блокировка скролла, закрытие по Escape,
  фокус ставится на кнопку закрытия;
* FAQ на нативных `<details>` — работает без JS и корректно читается скринридером;
* шкалы страха продублированы цифрой и `aria-label`, бегущая строка продублирована `sr-only` текстом;
* глитч не мешает чтению: он применяется к декору и к числам, базовый текст остаётся на месте;
* формы: у каждого поля есть `<label>`, `aria-invalid`, `aria-describedby` и подсказка.

---

## 4. Производительность

Замер на 375 px, production-сборка:

| Метрика | Значение | Порог | Итог |
|---|---|---|---|
| LCP | 1536 мс (эмуляция мобильного, после self-hosted шрифтов и preload) | ≤ 2500 | ✅ |
| CLS | 0.0000 | ≤ 0.1 | ✅ |
| TTFB | 34 мс | ≤ 800 | ✅ |
| Длина самой долгой задачи | 59 мс | — | ✅ |
| Запросов / вес | 18 запросов, 355 КБ сжатыми | — | ✅ |
| JS первого экрана | 179 КБ (shared 103 КБ) | — | ✅ |

**Крупнейшая находка:** LCP-элемент — абзац hero, и он «доотрисовывался» на 1.4 с позже FCP,
потому что веб-шрифт Oswald подгружался с внешнего CDN и подменял системный шрифт уже после
первого пейнта. Перевели шрифты на `next/font` (self-hosted, 17 woff2 в сборке): сторонние
запросы исчезли, второй LCP-пейнт больше не вызывается.

Самые тяжёлые ресурсы после правок: hero-картинка 146 КБ (в формате webp/avif), три общих
JS-чанка 45–53 КБ, CSS 12 КБ. Ни один ресурс не грузился дольше 16 мс.

---

## 5. QA

### Автотесты

`scripts/smoke-test.ps1` — **29 проверок, 0 провалов**:

* страницы `/`, `/booking`, `/quests/*`, `/admin`, `/sitemap.xml`, `/robots.txt` (200) и 404 на несуществующий квест;
* canonical, `LocalBusiness`, `FAQPage` в HTML;
* занятость: 8 слотов в дне, пометка прошедшего времени, сводка по месяцу;
* создание брони: сумма (6 × 3 500 − 3 500 акции = **17 500 ₸**), состав чека;
* защита от дублей;
* валидация: мусорная заявка → 422 с ошибками по игрокам, дате и телефону;
* админка: анонимный список → 401, вход по паролю, список и статистика, смена статуса.

### Инструментальные проверки

| Шаг | Результат |
|---|---|
| `npx tsc --noEmit` | ошибок нет |
| `npx eslint .` | ошибок и предупреждений нет |
| `npm run build` | успешно, 13 маршрутов |
| Консоль браузера на `/`, `/booking`, `/quests/ritual` | 0 ошибок, 0 предупреждений, 0 запросов 404 |

### Баги, найденные и исправленные в ходе QA

1. **Cookie админки с флагом `secure` в production на http** — вход был невозможен при локальном
   проде и на хостинге без TLS. Теперь флаг зависит от реальной схемы адреса.
2. **Контраст красного текста 2.86:1** — исправлено токеном.
3. **Тап-таргеты 15–40 px** — исправлено (44 px).
4. **Залипшая подпись кастомного курсора** после закрытия интро.
5. **`TypeError: target.closest is not a function`** в обработчике курсора, когда целью
   pointer-события оказывался не элемент (document, текстовая нода).
6. **Тест ловил «все слоты заняты» вечером** — это корректное поведение системы, но тест был
   зависим от времени; сделан независимым от часа запуска.
7. **Ошибка парсинга PowerShell-скрипта с кириллицей** — файл сохраняется в UTF-8 с BOM.
8. **Обрезка текста липкой панелью** на 320–430 px.
9. **Второй LCP-пейнт** из-за внешних шрифтов.

---

## 6. Раунд 2: усиление атмосферы (персонажи, кадры, эффекты)

### Что добавлено

| Новое | Детали |
|---|---|
| Секция «Персонажи» (`#characters`) | 4 портрета актёров с ролями, уровнем контакта и деталью-крючком; ниже — 4 реальных правила работы актёров |
| Галерея «Изнутри» (`#inside-photos`) | 5 атмосферных кадров локаций с полноэкранным просмотром |
| «Испытание страха» (`#fear-test`) | интерактивный ползунок 1–4: описание того, что реально происходит на каждом уровне, и переход в бронь с выбранным режимом (`/booking?fear=hard`) |
| Роли на странице квеста | блок «Роли этого сценария» + 3 кадра локации (`quest-companions.tsx`) |
| 10 новых изображений | 4 портрета персонажей, 5 кадров локаций, 1 ассет вспышки «глаза в темноте» (всего в проекте 20) |
| Эффекты | туман, редкая вспышка света, следящие глаза, jump scare, затемнение краёв кадра, капли крови, хроматическая аберрация, шёпот, ползущая линия, сердцебиение, пульсирующая рамка |
| Звук | добавлены сердцебиение и удар на jump scare (по-прежнему только после клика) |

### Проверено в браузере (QA-раунд 2 и 3)

| Проверка | Результат |
|---|---|
| Jump scare появляется при наведении на карточку | да, вспышка ~0.26 с |
| Кулдаун jump scare | 6 секунд подтверждены: 3 быстрых наведения подряд → 0 вспышек |
| Не блокирует клики | да, `pointer-events: none`; `elementFromPoint` во время вспышки возвращает элемент под ней |
| Полноэкранный просмотр кадров | открывается, стрелки листают (01/05 → 02/05), Escape закрывает, скролл фона блокируется |
| Ползунок страха | 04/04 → «Хард», `aria-valuetext` меняется, ссылка становится `/booking?fear=hard`, на шаге 1 режим «Хард» с `aria-pressed="true"` |
| Горизонтальный оверфлоу | нет ни в одной новой секции (`#characters` 0, `#fear-test` 0, страница 365 = 365 на 375 px) |
| Перекрытие фиксированной кнопкой | на максимальном скролле зазор 56 px, пересечений с текстом 0 |
| Декоративные слои | все `pointer-events: none`; CTA в hero на 375 не перекрыты |
| Консоль и сеть | 0 ошибок, 0 предупреждений, все запросы 200 |

### Найдено и исправлено в этом раунде

1. **Резкое падение контраста из-за эффекта затемнения (критично).** Прогрессивное затемнение
   складывалось с виньеткой и давало в нижней части страницы эффективный контраст
   **1.34:1** (метка «Вопросы»), 1.50–2.69:1 у остальных элементов — то есть почти весь
   текст внизу главной становился плохо читаемым.

   Причина: формула WCAG добавляет к яркости 0.05, поэтому при затемнении сильнее ~0.3
   даже светлый текст на чёрном уходит ниже 4.5:1 — светлое теряет больше, чем тёмное.

   Исправление: виньетка стала «уголковой» (центральные 55 % радиуса не затемняются,
   максимум по краям 0.24), затемнение краёв теперь прозрачно в центральных 62 % кадра,
   акцентный красный поднят с `#dd5560` до `#e0626b`, вторичные серые (`ash-text`, `bone-dim`)
   осветлены, подписи на SVG-схеме локаций увеличены с 9 до 10–11 px и осветлены.

   Повторный замер: **минимум 5.02:1**, ниже нормы не осталось ничего.

   | Элемент | Кегль | Было | Стало |
   |---|---|---|---|
   | метка «Вопросы» | 10 px | 1.34 | **5.02** |
   | метка «Перед входом» | 10 px | 1.58 | **5.02** |
   | подпись «Карательная психиатрия · июнь 2020» | 9 px | 1.50 | **5.37** |
   | ссылка «источник» | 9 px | 2.00 | **5.52** |
   | заголовок «Безопасность» | 18 px | 2.04 | **12.30** |
   | абзац FAQ | 15 px | 1.93 | **9.90** |
   | подписи SVG-схемы | 10–11 px | 4.28 | **7.49–14.17** |

2. **Excel-подобная ловушка в проектировании эффектов**: «чем глубже скролл, тем темнее»
   звучит хорошо, но в реальности ломает читаемость. Оставлено ровно столько затемнения,
   сколько не мешает тексту — проверено числами, а не на глаз.

---

## 7. Раунд 3: мобильный приоритет и логотип

### Логотип

Разработан знак (приоткрытая дверь в бетонной раме, щель света, капля на пороге) и
три лок-апа: горизонтальный, вертикальный и одноцветный. Компонент вставлен инлайном,
favicon заменён на упрощённую версию знака. Проверено на 320 px: не обрезается,
читается, контраст подписи 10.99:1.

### Проверено в браузере (эмуляция тача: `hover:none`, `pointer:coarse`, `maxTouchPoints=5`)

| Проверка | Результат |
|---|---|
| Горизонтальный скролл, ширины 320–430 | `scrollWidth == clientWidth` на всех шести ширинах; принудительный `scrollTo(600)` → `scrollX = 0` |
| Обрезка заголовков | нет ни на одной ширине (проверены h1/h2/h3) |
| Hover-слои на тач-устройстве | «ENTER EXPERIENCE» (6 карточек), иконка раскрытия кадра (5), плашка «смотрит на вас» (4) — **видны**, opacity 1; на десктопе в тех же местах opacity 0 |
| Нижняя панель брони, 6 ширин | высота стабильна 71 px; зазор «текст ↔ WhatsApp» 23.5–119 px, «WhatsApp ↔ CTA» 8 px; перекрытий 0 из 75/192 проверенных точек; обрезки текста нет |
| Меню 320×568 | контейнер прокручивается (scrollHeight 626 > clientHeight 499), свайпом достигнут низ, кнопка брони и телефон целиком видны, запас снизу 56 px |
| Строка «Атмосферный звук» | состояние отображается текстом «вкл/выкл», `aria-pressed` переключается, работает |
| Свайп в календаре | Сентябрь → Октябрь → Сентябрь (реальными touch-событиями) |
| Свайп в просмотре кадров | 01/05 → 02/05 → 01/05 |
| Бронь до конца на 375 | `POST /api/bookings → 201`, бронь `HC-2U3E9LZZ`, экран «Заявка принята»; на `/booking` фиксированной панели нет — форма не перекрыта |
| Предзагрузка hero | на главной грузится **только** кадр под текущую ширину (45 мс), на `/booking` preload отсутствует, warning «preloaded but not used» исчез |
| Консоль | `/` и `/booking` — 0 ошибок, 0 предупреждений, 0 запросов 404 |

### Найдено и исправлено в этом раунде

| № | Дефект | Причина | Исправление |
|---|---|---|---|
| 1 | На 320×568 в мобильном меню кнопка «Забронировать игру» была обрезана на 33 px, телефон недостижим | прокручивалась только навигация, а блок действий стоял вне скролла и не помещался в экран | прокручивается весь блок целиком (пункты + действия), увеличен нижний запас; повторно: запас 56 px, всё достижимо |
| 2 | Подпись «Бронь онлайн» на 320 px переносилась в две строки, панель росла до 83 px | узкая колонка и длинный текст | подпись скрыта до 380 px, цена сокращается до «от 3 500 ₸», добавлен `truncate`; высота стабилизировалась на 71 px |
| 3 | **Цена перекрывала иконку WhatsApp на 320 и 360 px** (50 px и 10 px текста под кнопкой) | после `white-space: nowrap` текст стал шире своей колонки и вылезал поверх соседнего элемента | сокращение текста по брейкпоинту + короткая кнопка (иконка скрыта до 380 px) + `truncate` как страховка; повторный замер: перекрытий 0 |
| 4 | 150 КБ hero-кадр предзагружался и на странице брони | preload стоял в общем layout | предзагрузка перенесена на главную, в layout её нет |
| 5 | У переключателя звука не было текстового состояния | только `aria-pressed` и иконка | добавлен индикатор «вкл/выкл» |
| 6 | Параллакс hero и анимации зерна/тумана работали и на телефонах | эффекты не были разведены по устройствам | параллакс включается только с точным указателем и шириной ≥1024 px; на мобильных отключены анимация зерна, второй слой тумана и затемнение краёв |

### Что подтвердилось как преимущество

* `env(safe-area-inset-*)` + `viewport-fit=cover`: шапка и нижняя панель не залезают
  под системные зоны iPhone;
* поля формы с кеглем 16 px: iOS не зумит страницу при фокусе;
* свайпы календаря и галереи работают реальными touch-событиями, вертикальный скролл
  не перехватывается (свайп считается только при горизонтальном преобладании).

---

## 8. Раунд 4: фоновые кадры и типографика мелкого текста

### Что добавлено

10 новых кадров (3 фактуры стен, 7 атмосферных полос) — всего в проекте 30 изображений.
Фоны легли под 11 секций, полосы — между секциями, плюс шапка страницы бронирования,
страница 404 и по одной полосе на каждой странице квеста (свой кадр для каждого сценария).

### Проверено

| Проверка | Результат |
|---|---|
| Контраст: замерено элементов | **344** (главная 1440/375, `/booking` 375, 404 1440, `/quests/ritual` 1440/375), учитывались все слои: кадр + градиенты + виньетка + затемнение + зерно |
| Ниже 4.5:1 (нормативный расчёт) | **0 элементов**, минимум **5.37** (404) |
| Влияние новых кадров на контраст | максимум **0.14** пункта, в среднем 0.013 — то есть фоны почти не трогают читаемость |
| Цитаты на полосах | 5.80–5.97 |
| Кликабельность в секциях с фонами | hit-тест CTA каталога, кнопки на полосе, шапки, «Маршрут в 2ГИС», WhatsApp в FAQ — OK на 320/375/1440, перехватов нет |
| Горизонтальный скролл | нет на 320/375/1440 |
| Ленивая загрузка фонов | в HTML 32 изображения, `eager` — только hero; все кадровые теги `loading="lazy"`; до прокрутки грузится 8 картинок, дальше по 2–7 на шаг (все 30 сразу не тянутся) |
| Производительность | LCP **1608 мс**, CLS **0.0000**, TTFB **39 мс** |
| Консоль | 0 ошибок, 0 предупреждений, 0 ответов ≥400 на трёх страницах |

### Отдельная находка: мелкий моно-текст на экранах 1x

Замер в пикселях (а не по CSS) показал, что надписи 9–10 px при плотности экрана 1x
читаются слабее расчётного: сглаживание «съедает» тонкие штрихи моношрифта.
Нормативный контраст при этом всегда был в норме (5.66–5.86), поэтому это не нарушение
WCAG, а реальное качество картинки на обычных мониторах.

Что сделано по шагам (каждый шаг подтверждён замером):

| Шаг | Элементов ниже 4.5 (пиксельный замер, 1440) | Минимум |
|---|---|---|
| Исходно | 47 | 3.22 |
| Убрано grayscale-сглаживание + начертание моно 500 | 36 | 3.51 |
| Все подписи 9 px → 10 px + моно-метки 11 px | **28** | **3.93** |

Итог: количество проблемных мест сократилось на 40 %, минимум вырос с 3.22 до 3.93.
Остаток — 9–11 px служебные подписи (`#948f86`, `#e0626b`) на самых тёмных секциях.
На реальных телефонах (DPR 2–3) проблемы нет: там замер даёт 5.4–5.8.
Дальнейшее «выжимание» пиксельной метрики потребовало бы отказаться от мелкого
моно-кегля в дизайне — это уже вопрос стиля, а не доступности.

### Что осталось от QA

* `tex-rust.jpg` встречается на главной дважды: браузер берёт файл из кеша
  (второй запрос не скачивается), в resource timing это выглядит как два обращения;
* на полосах без кнопки (сейчас таких нет) пауза превращалась бы в тупик — поэтому
  кнопка есть на каждой полосе;
* 3 фактуры стен запрашиваются в разрешении 1200 px и сжаты до q70: на глаз разницы
  под 12 % непрозрачности нет, экономия ~40 % трафика.

---

## 9. Раунд 5: лимит телефона, упрощение брони, анимация текста

### Что сделано

* поле телефона с маской и жёстким лимитом 11 цифр («8» → «+7», вставка
  обрезается, 12-я цифра не вводится, подсказка «осталось N цифр»);
* бронь упрощена: компактный выбор уровня страха, свёрнутый блок доп. услуг
  («шаг 2 короче на 497 px»), три узкие кнопки способа связи, липкая кнопка
  действия на мобильных;
* анимация ключевой фразы «Ты готов войти?» по буквам + строка-шёпот;
* правки по аудиту блоков (кликабельные слоты, «Выбрать время» вместо
  «Забронировать», плашка «рекомендуем в первый раз», свёрнутые правила).

### Найдено и исправлено

| № | Дефект | Причина | Исправление и проверка |
|---|---|---|---|
| 1 | Липкая кнопка в брони не залипала: на шаге 3 при `scrollY=1500` кнопка была целиком ниже экрана (top 1103 при экране 812) | `overflow-x: hidden` на `html`/`body` делает скролл-контейнером `body`, у которого `clientHeight === scrollHeight` → диапазон залипания нулевой | `overflow-x: clip` (+ фолбэк `hidden` в `@supports not`): окно залипания 351…1865 на шаге 2 и 375…1860 на шаге 3, кнопка видна во всех 8 проверенных точках, зазор до последнего поля 24 px |
| 2 | Анимация текста не запускалась при «прыжке» скролла: после `scrollTo(0, maxScroll)` все 15 букв оставались `opacity: 0` | `useInView(once)` не срабатывает, если элемент «проскочил» мимо экрана; фолбэк был только у `Reveal` | логика вынесена в общий хук `useRevealed` (IO + проверка `rect.top < innerHeight` на каждом кадре): через 2 с после прыжка букв ниже порога — 0 |
| 3 | `aria-describedby="hint-phone"` ссылался на несуществующий id — подсказка поля телефона не озвучивалась скринридером | подсказка переехала внутрь `PhoneInput`, id потерялся | id возвращён; проверено: `sameNode: true`, скринридер читает «11 цифр: 8 или +7 в начале» |
| 4 | Бегущая строка растягивала `scrollWidth` документа до 7182 px | у контейнера не было `overflow-hidden`; вылезание маскировалось `overflow-x: hidden` | контейнер получил `overflow-hidden`; после перехода на `clip` вылезания нет ни на одной ширине (`scrollWidth === clientWidth` на 320/375/390/1440 для трёх страниц) |
| 5 | Подпись «Подтвердить бронь · 15 000 ₸» переносилась в две строки на 375 px | фиксированный кегль кнопки | кегль и отступы уменьшены на узких экранах (`min-[400px]`) |

### Проверено

| Проверка | Результат |
|---|---|
| Маска телефона (375, реальные события ввода и вставки через ClipboardEvent) | `8912345678901234` → `+7 912 345-67-89`; вставка `8 (701) 123-45-67 доб. 12` → `+7 701 123-45-67`; вставка `+7 777 399 98 43` → `+7 777 399-98-43`; 12-я цифра не вводится; Backspace восстанавливает формат |
| Бронь до конца с номером «как человек» | `87015554433` → `+7 701 555-44-33` → «Заявка принята», `HC-79KC7BKQ`; повторная отправка вернула тот же id (дедупликация жива) |
| Анимация текста | «Ты готов войти?» — 15 букв, ниже порога 0 через 2 с и через 6 с; «Это не комната с замками» — 24 буквы, то же; `sr-only` присутствует; шёпот появляется и гаснет; контраст 14.5:1 |
| Высота шага 2 | 1662 px со свёрнутым блоком опций → 2159 px с раскрытым (−497 px в пользу короткой формы) |
| Аудит-правки | 3 ссылки на слоты ведут в форму с выбранным квестом; «Выбрать время» работает; плашка «рекомендуем» только у «Лайт»; «Перед входом» — 4 карточки (54 px) → раскрыть → 8 карточек (296 px) |
| Регрессия | нет горизонтального скролла на 320/375/390/1440 (12 комбинаций), консоль чистая, `tsc`/`eslint`/`build` без замечаний, smoke-тесты 29/29 |

---

## 10. Остаточные риски и что подтвердить перед публикацией

| Риск | Что нужно |
|---|---|
| Юридический: используются название, цены, адреса, телефон и отзывы реальной площадки | письменное согласие владельца до публикации |
| Изображения сгенерированы, а не сняты на площадке (20 файлов) | заменить на реальные фото команд и локаций (иначе площадка выглядит иначе, чем в жизни) |
| Роли персонажей («Санитар», «Тот, под полом», «Ночная смена», «Незваный гость») — художественная подача | подтвердить названия у владельца или убрать блок `CHARACTERS` — раскрытие сценариев может противоречить планам площадки |
| Jump scare и вспышки — часть впечатления, но кому-то противопоказаны | предупреждение о вспышках стоит добавить в «Перед входом», если площадка считает это нужным; сейчас эффект отключается только по системной настройке «уменьшить движение» |
| `fear` / `difficulty` и 3 сюжета — редакторские | подтвердить у владельца, при необходимости поправить `lib/content.ts` |
| Занятость — демо-расписание | подключить CRM/календарь, иначе слоты не совпадут с реальными |
| Хранилище — файл/память | подключить БД до боевого запуска (иначе на serverless брони теряются при перезапуске) |
| `ADMIN_PASSWORD` по умолчанию `horror-clinic` | задать свой пароль и `ADMIN_SESSION_SECRET` |
| Нет e2e-тестов (Playwright) | добавить, если правки будут частыми: форма брони — критичный путь |
| Тексты «как найти вход» и правила съёмки | подтвердить у администраторов площадки |
